app:
  frontend:
    url: https://trello-clone-victor.vercel.app
cors:
  originPatterns: https://trello-clone-frontend-prod.onrender.com,http://localhost:3000,https://trello-clone-victor.vercel.app
logging:
  level:
    org.springframework.security: DEBUG
security:
  jwt:
    private:
      key: ${JWT_PRIVATE_KEY}
    public:
      key: ${JWT_PUBLIC_KEY}
server:
  port: ${PORT}
spring:
  application:
    name: trello-clone
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  mail:
    host: smtp.gmail.com
    port: 465
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enabled: true
      mail.smtp.starttls.required: true
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
